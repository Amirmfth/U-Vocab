"use server";
import { redirect } from "next/navigation"; import { db } from "@/lib/db"; import { analyzeGermanLexeme } from "@/lib/ai/analyze-word";
export async function createLexeme(formData:FormData){const input=String(formData.get("word")??"").trim();if(!input)throw new Error("A German word or phrase is required.");const a=await analyzeGermanLexeme(input);const lexeme=await db.lexeme.create({data:{lemma:a.lemma,normalized:a.lemma.toLocaleLowerCase("de-DE"),partOfSpeech:a.partOfSpeech,article:a.article,gender:a.gender,plural:a.plural,translations:{create:[...a.englishMeanings.map(text=>({language:"en",text})),...a.persianMeanings.map(text=>({language:"fa",text}))]},patterns:{create:a.patterns},examples:{create:a.examples.map(x=>({...x,generatedByAi:true}))}}});redirect(`/vocabulary/${lexeme.id}`);}